{
  "//": "Profile: run the API on the host against a simulator on localhost.",
  "//usage": "ASPNETCORE_ENVIRONMENT=Local dotnet run --project src/OpcMonitor.Api",

  "Opc": {
    "EndpointUrl": "opc.tcp://localhost:62541"
  }
}
